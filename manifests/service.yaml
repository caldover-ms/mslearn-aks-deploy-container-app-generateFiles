apiVersion: v1
kind: Service
metadata:
  name: test-workflow1
  namespace: fleet-member-ca-westus2-cluster1-fm
  labels:
    app.kubernetes.io/name: test-workflow1
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: test-workflow1
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000